import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    flatList: {
        flex: 1
    },
    emptyView: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
})